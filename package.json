{
  "name": "Let's",
  "version": "0.0.2",
  "description": "Where awesome people invite each other to do awesome things.",
  "scripts": {
    "start": "meteor --settings settings-development.json",
    "staging": "meteor deploy lets-staging.meteor.com --settings settings-development.json",
    "production": "meteor deploy lets-production.meteor.com --settings settings-production.json"
  }
}
